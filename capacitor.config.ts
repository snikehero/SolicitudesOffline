import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tdcon.solicitudesoffline',
  appName: 'Solicitudes Offline',
  webDir: 'dist/client',
  android: {
    backgroundColor: '#f3f6fa',
  },
};

export default config;
